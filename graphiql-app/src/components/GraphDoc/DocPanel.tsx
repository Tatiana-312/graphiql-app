import styles from './DocPanel.module.scss';

const DocPanel = () => {
  return (
    <div className={styles.panel}>
      <button className={styles.button}>Doc</button>
    </div>
  );
};

export default DocPanel;
